# Kong Bank Lab Handover

Date prepared: 2026-07-06

This is the restart point for fresh Codex and ChatGPT Pro sessions on
2026-07-07.

## Repo State

- Repo: `/home/boris/code/soyspray`
- Branch: `kong-goals-foundation`
- Remote: `origin`
- Latest pushed commit before this handover file was created: `fadc0d8`
- Fresh sessions must confirm current branch HEAD with
  `git rev-parse --short HEAD`.
- Current Kubernetes context observed at handover time:
  `kubernetes-admin@cluster.local`

## What Was Reached

- `goal-000-repo-foundation`: complete.
- `goal-001-platform-prereqs`: complete.
- `goal-002-kong-oss-baseline`: runtime-verified.
- `goal-003-synthetic-bank-apis`: implemented and approved by ChatGPT Pro as a
  local-only checkpoint.
- `gate-003-synthetic-api-runtime-apply-and-smoke`: local-only gate package
  implemented, validated, pushed, and approved by ChatGPT Pro.
- The post-goal-005 save point rule is recorded in
  `soydocs/kong-bank-lab/post-goal-005-savepoint.md` and accepted by Pro.

## What Is Left Next

Do not start `goal-004-auth-rate-limit-security` yet.

The next required action is the guarded runtime apply and smoke gate for goal
003. It needs explicit user approval before any cluster-mutating command runs.

The approval template is recorded in:

- `soydocs/kong-bank-lab/current-state.md`
- `docs/runbooks/synthetic-api-runtime-apply-and-smoke.md`

After approval, Codex should run:

```sh
export BANKLAB_ALLOW_CLUSTER_MUTATION=true
export BANKLAB_TARGET_CONTEXT=kubernetes-admin@cluster.local
platform/kong/synthetic-apis/scripts/synthetic-api-runtime-apply-and-smoke.sh
```

The gate is successful only when `make goal003-runtime-ready` passes.

## Evidence At Handover

Commands checked at handover:

```text
git status --short --branch
git rev-parse --short HEAD
kubectl config current-context
make validate-synthetic-api-runtime-gate
make goal003-runtime-ready
```

Observed results:

- Branch is clean and tracking `origin/kong-goals-foundation`.
- `git rev-parse --short HEAD`: `fadc0d8`
- `kubectl config current-context`: `kubernetes-admin@cluster.local`
- `make validate-synthetic-api-runtime-gate`: pass
- `make goal003-runtime-ready`: expected fail-closed because runtime apply and
  smoke evidence has not been produced.

The failure marker was:

```text
Missing goal003 runtime-ready marker in reports/goal-003-summary.md: Cluster changes performed: synthetic bank APIs applied
```

This is expected. It is not a bug.

## Important Boundary

The long-running Codex goal was blocked at the approval boundary so it stops
auto-continuing into the same missing-permission state. This does not mean the
Kong programme is complete.

To continue tomorrow, start a fresh Codex session from this repo and give Codex
the explicit gate approval from `current-state.md` if the cluster mutation is
approved.

## ChatGPT Pro Prompt For 2026-07-07

Paste this into the fresh ChatGPT Pro Kong conversation:

```text
We are restarting the Kong bank-lab programme from a saved repo handover.

Repo: /home/boris/code/soyspray
Branch: kong-goals-foundation
Latest pushed commit before the handover file was created: fadc0d8
Fresh Codex should verify the current branch HEAD with git rev-parse --short HEAD.
Current saved handover file: soydocs/kong-bank-lab/handover-2026-07-06.md
Current state file: soydocs/kong-bank-lab/current-state.md

Reached:
- goal-000-repo-foundation complete
- goal-001-platform-prereqs complete
- goal-002-kong-oss-baseline runtime-verified
- goal-003-synthetic-bank-apis implemented and approved as local-only
- gate-003-synthetic-api-runtime-apply-and-smoke local-only package implemented, validated, pushed, and approved by Pro

Current blocker:
- Do not start goal-004 yet.
- goal003 runtime apply/smoke has not run.
- make validate-synthetic-api-runtime-gate passes.
- make goal003-runtime-ready fails closed as expected because runtime evidence is missing.
- Current Kubernetes context is kubernetes-admin@cluster.local.

Please review this state and confirm the next repo-safe action.

If you agree that the next action is still gate-003-synthetic-api-runtime-apply-and-smoke, tell us exactly what runtime evidence you want after Codex runs the guarded command sequence.

After Codex reports the runtime evidence, either:
1. approve goal003 as runtime-verified and provide the full body for goal-004-auth-rate-limit-security, including any corrections to carry forward, or
2. give concrete corrections and the exact validation/evidence commands to rerun.

Do not get stuck re-validating indefinitely. If evidence is insufficient, say exactly what command or file is missing and how to progress.

Programme control:
- After goals 004 and 005 are complete and approved, create a save point for both Codex and Pro.
- Goal 006 will start in fresh contexts after that save point.
```

## Codex Restart Note

Fresh Codex should start by reading:

1. `soydocs/kong-bank-lab/handover-2026-07-06.md`
2. `soydocs/kong-bank-lab/current-state.md`
3. `soydocs/kong-bank-lab/goals/gate-003-synthetic-api-runtime-apply-and-smoke.md`
4. `docs/runbooks/synthetic-api-runtime-apply-and-smoke.md`

Then confirm the branch and context before running any command:

```sh
git status --short --branch
git rev-parse --short HEAD
kubectl config current-context
```

Only run the guarded runtime gate after explicit user approval matching
`current-state.md`.
