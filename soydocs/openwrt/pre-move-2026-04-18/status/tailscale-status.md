Live capture from `tailscale status`, sanitized.

- Router node present in the tailnet as `openwrt`
- Local Tailscale IPv4: `<redacted>`
- Peer `mox` is reachable directly on the LAN
- Peer `node-0` is present in the tailnet
- Android phone peer exists but was offline during capture
