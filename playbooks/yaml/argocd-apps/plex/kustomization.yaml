# playbooks/yaml/argocd-apps/plex/kustomization.yaml
# Kustomization for Plex
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # Commented out all custom configurations for manual setup
  # - initial-config-configmap.yaml
  # - bootstrap-configmap.yaml
  # - poststart-configmap.yaml

commonLabels:
  app: plex
  component: main
