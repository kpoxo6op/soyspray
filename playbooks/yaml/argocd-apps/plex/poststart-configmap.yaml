# playbooks/yaml/argocd-apps/plex/poststart-configmap.yaml
# Replaces Personal Media agent with default Plex TV Series agent by omitting --agent
# Waits for PlexOnlineToken after claim to avoid race
# Forces metadata refresh for the TV library before scanning
# Uses --server-token for all scanner actions; keeps /data PVC flow intact

apiVersion: v1
kind: ConfigMap
metadata:
  name: plex-poststart
  namespace: media
  labels:
    app: plex
  annotations:
    argocd.argoproj.io/sync-wave: "0"
data:
  poststart.sh: |-
    set -Eeuo pipefail

    echo "[postStart] Waiting for Plex to initialize..."

    timeout=300
    counter=0
    while [ $counter -lt $timeout ]; do
        if curl -s http://127.0.0.1:32400/identity >/dev/null 2>&1; then
            echo "[postStart] Plex identity endpoint is available"
            break
        fi
        echo "[postStart] Waiting for Plex... ($counter/$timeout)"
        sleep 5
        counter=$((counter + 5))
    done

    if [ $counter -ge $timeout ]; then
        echo "[postStart] Timeout waiting for Plex to start"
        exit 1
    fi

    if [ -n "${PLEX_ACCOUNT_TOKEN}" ]; then
        IDENTITY_RESPONSE=$(curl -fsS "http://127.0.0.1:32400/identity" 2>/dev/null || true)
        if echo "$IDENTITY_RESPONSE" | grep -q 'claimed="1"'; then
            echo "[postStart] Server already claimed - skipping claim process"
        else
            echo "[postStart] Server not claimed - starting claim process..."
            for attempt in 1 2 3; do
                echo "[postStart] Claim attempt $attempt/3"
                CLAIM_RESPONSE=$(curl -fsS "https://plex.tv/api/claim/token?X-Plex-Token=${PLEX_ACCOUNT_TOKEN}" 2>/dev/null || true)
                CLAIM_TOKEN=$(echo "$CLAIM_RESPONSE" | grep -o 'token="[^"]*"' | cut -d'"' -f2)
                if [ -n "$CLAIM_TOKEN" ]; then
                    echo "[postStart] Claim token acquired"
                    curl -fsS -X POST "http://127.0.0.1:32400/myplex/claim?token=${CLAIM_TOKEN}" >/dev/null 2>&1 || true
                    verify_counter=0
                    while [ $verify_counter -lt 30 ]; do
                        if curl -fsS "http://127.0.0.1:32400/identity" 2>/dev/null | grep -Fq 'claimed="1"'; then
                            echo "[postStart] Claim successful"
                            break 2
                        fi
                        sleep 2
                        verify_counter=$((verify_counter + 2))
                    done
                    echo "[postStart] Claim not reflected yet; retrying"
                else
                    echo "[postStart] Failed to parse claim token response"
                fi
                sleep 5
            done
            if ! curl -fsS "http://127.0.0.1:32400/identity" 2>/dev/null | grep -Fq 'claimed="1"'; then
                echo "[postStart] Warning: Auto-claim failed; continuing bootstrap"
            fi
        fi

        PREF_FILE="/config/Library/Application Support/Plex Media Server/Preferences.xml"
        echo "[postStart] Waiting for PlexOnlineToken to be persisted..."
        tok_wait=0
        while [ $tok_wait -lt 60 ]; do
            if grep -q 'PlexOnlineToken="' "$PREF_FILE" 2>/dev/null; then
                echo "[postStart] PlexOnlineToken detected"
                break
            fi
            sleep 2
            tok_wait=$((tok_wait + 2))
        done
    else
        echo "[postStart] No PLEX_ACCOUNT_TOKEN provided, skipping server claiming"
    fi

    DB_FILE="/config/Library/Application Support/Plex Media Server/Plug-in Support/Databases/com.plexapp.plugins.library.db"
    counter=0
    while [ $counter -lt 60 ]; do
        if [ -f "$DB_FILE" ]; then
            echo "[postStart] Database file exists: $DB_FILE"
            break
        fi
        echo "[postStart] Waiting for database creation... ($counter/60)"
        sleep 2
        counter=$((counter + 2))
    done

    if [ ! -f "$DB_FILE" ]; then
        echo "[postStart] Warning: Database file not found, proceeding anyway"
    fi

    export LD_LIBRARY_PATH=/usr/lib/plexmediaserver
    export PLEX_MEDIA_SERVER_APPLICATION_SUPPORT_DIR="/config/Library/Application Support"

    echo "[postStart] Ensuring TV Shows library exists with Plex TV Series agent..."
    if ! "/usr/lib/plexmediaserver/Plex Media Scanner" --server-token "${PLEX_ACCOUNT_TOKEN}" --list | grep -Fq "TV Shows"; then
        "/usr/lib/plexmediaserver/Plex Media Scanner" --server-token "${PLEX_ACCOUNT_TOKEN}" --add-section "TV Shows" --type 2 --location "/data" --lang en
        echo "[postStart] TV Shows library created"
    else
        echo "[postStart] TV Shows library already exists"
    fi

    echo "[postStart] Forcing metadata refresh on TV Shows..."
    SECTIONS_XML="$(curl -fsS "http://127.0.0.1:32400/library/sections?X-Plex-Token=${PLEX_ACCOUNT_TOKEN}")"
    TV_LINE="$(printf '%s' "$SECTIONS_XML" | tr '\n' ' ' | sed -n 's/.*<Directory[^>]*title="TV Shows"[^>]*>.*/\0/p')"
    TV_KEY="$(printf '%s' "$TV_LINE" | grep -o 'key="[^"]*"' | cut -d'"' -f2)"
    if [ -n "$TV_KEY" ]; then
        curl -fsS "http://127.0.0.1:32400/library/sections/${TV_KEY}/refresh?force=1&X-Plex-Token=${PLEX_ACCOUNT_TOKEN}" >/dev/null 2>&1 || true
    fi

    echo "[postStart] Performing initial scan of /data..."
    "/usr/lib/plexmediaserver/Plex Media Scanner" --server-token "${PLEX_ACCOUNT_TOKEN}" --scan --directory "/data" || true

    echo "[postStart] Library setup complete"
