apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  namespace: default
  annotations:
    # Enable external-dns to manage this record
    external-dns.alpha.kubernetes.io/hostname: test.soyspray.vip
    # Use Tailscale loadbalancer class
    service.kubernetes.io/load-balancer-class: "tailscale"
spec:
  ingressClassName: nginx
  rules:
  - host: test.soyspray.vip
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-service
            port:
              number: 80
