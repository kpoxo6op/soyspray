# playbooks/yaml/argocd-apps/sonarr/kustomization.yaml
# Kustomize configuration for Sonarr
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc-config.yaml
  - initial-config-configmap.yaml

commonLabels:
  app: sonarr
  component: main
