# playbooks/yaml/argocd-apps/object-storage/kustomization.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - secret.yaml
  - pvc-data.yaml
  - deployment.yaml
  - service.yaml

