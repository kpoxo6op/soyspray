# jackett

## Purpose
Proxy/aggregator for private trackers. It feeds search results to Readarr.

## Minimal Validation (Before Moving On)
• Web UI is reachable.
• At least one private tracker is configured and returns results on test search.

## Ladder Progression
**Next Step**: Once validated, proceed to deploy `readarr`.
