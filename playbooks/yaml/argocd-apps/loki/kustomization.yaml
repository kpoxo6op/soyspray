apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - loki-object-storage-secret.yaml
  - lokistack.yaml
  - rulerconfig.yaml
  - loki-externalname-service.yaml
  - loki-servicemonitor.yaml
  - loki-alerting-rules.yaml

