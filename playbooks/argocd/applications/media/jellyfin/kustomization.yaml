apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - pvc-config.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - bootstrap-scripts-configmap.yaml
  - bootstrap-job.yaml

commonLabels:
  app: jellyfin
