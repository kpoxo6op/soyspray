apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - storageclass-local-media.yaml
  - pv-downloads.yaml
  - pvc-downloads.yaml
  - deployment.yaml
  - service.yaml
  - service-peers.yaml
  - ingress.yaml
  - pvc-config.yaml
  - configmap.yaml
  - bootstrap-job.yaml

commonLabels:
  app: qbittorrent
