apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: splunk
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: "https://github.com/kpoxo6op/soyspray.git"
    targetRevision: "exp/splunk-rest-pipeline"
    path: playbooks/argocd/applications/observability/splunk
  destination:
    server: "https://kubernetes.default.svc"
    namespace: splunk
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      retry:
        limit: 5
        backoff:
          duration: "10s"
          factor: 2
          maxDuration: "5m"
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
