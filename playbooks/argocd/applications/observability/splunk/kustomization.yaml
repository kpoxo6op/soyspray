apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: splunk

secretGenerator:
  - name: splunk-env
    envs:
      - .env
    options:
      disableNameSuffixHash: true

resources:
  - certificate.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
