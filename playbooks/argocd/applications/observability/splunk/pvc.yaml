apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: splunk-data
  namespace: splunk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
