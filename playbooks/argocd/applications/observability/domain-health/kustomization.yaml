apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: domain-health-exporter
    files:
      - domain-health-exporter.py

resources:
  - deployment.yaml
  - service.yaml
