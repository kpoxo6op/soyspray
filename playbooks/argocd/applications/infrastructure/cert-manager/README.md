# Certificate configuration source

The existing manifest path stays active during native adoption. Use the
[certificate app README](../../../../../apps/cert-manager-config/README.md) for
commands, ownership, bootstrap inputs, and verification. Kubespray retains the
cert-manager controller and CRDs. Move this source only after native adoption
preserves the existing certificates and keys.
