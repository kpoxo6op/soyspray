apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jira-home
  namespace: jira
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 30Gi
