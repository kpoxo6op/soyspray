# Set exact UTC PITR target before every restore drill.
# Restoring to last day with STANDARD storage: 2025-11-29
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-db
spec:
  bootstrap:
    recovery:
      recoveryTarget:
        targetTime: "2025-11-29 23:59:59+00:00"

