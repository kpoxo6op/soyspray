# Basic-auth secret for Immich DB user/password. Shared by both alias overlays.
apiVersion: v1
kind: Secret
metadata:
  name: immich-app-secret
type: kubernetes.io/basic-auth
stringData:
  username: immich
  password: immich
