# Generates six DB apps (a/b x initdb/prod/restore) without automation.
# These apps must not prune database resources when a generated app is removed.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: immich-db
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - letter: a
                - letter: b
          - list:
              elements:
                - role: initdb
                - role: prod
                - role: restore
  template:
    metadata:
      name: immich-db-{{letter}}-{{role}}
    spec:
      project: default
      destination:
        server: "https://kubernetes.default.svc"
        namespace: postgresql
      sources:
        - repoURL: "https://github.com/kpoxo6op/soyspray.git"
          targetRevision: "HEAD"
          path: playbooks/argocd/applications/database/cnpg/immich-db/overlays/{{role}}
          kustomize:
            nameSuffix: -{{letter}}
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
