# App Of Apps

These Argo CD `Application` manifests model the future app-of-apps structure.

They are staged manifests. Replace `REPLACE_WITH_REPO_URL` with the actual repo
URL before applying them. Local validation checks that the placeholder remains
obvious while this branch is not yet configured for live GitOps sync.

Goal 001 does not install Argo CD and does not apply these resources.

