# Argo CD Projects

These `AppProject` examples model platform, security, and tenant boundaries.

They intentionally use `REPLACE_WITH_REPO_URL` until the real repo URL is set.
They are not applied in goal 001.

