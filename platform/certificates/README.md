# Certificates

Certificate prerequisites for the Kong OSS bank lab live here.

Goal 001 defines the cert-manager structure and example issuers only. It does
not install cert-manager, issue Kong certificates, commit private keys, or
create production certificates.

