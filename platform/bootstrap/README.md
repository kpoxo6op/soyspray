# Bootstrap Helpers

This directory contains explicit, opt-in helper scripts for applying or checking
platform prerequisites.

Nothing here runs from `make validate`, CI, or the default `make` target.

Goal 001 does not apply these resources to the cluster.

