# Local kind Cluster Not Used

Goal 001 targets the existing home Kubespray cluster model, but local validation
must not require any cluster. A local kind cluster is not part of this goal.

Use static validation, kustomize rendering, pytest, and documentation builds for
the local gate.

