# Example only. This is not a real SOPS-encrypted secret and must not be applied.
apiVersion: v1
kind: Secret
metadata:
  name: non-deployable-sops-example
  namespace: platform-security
  annotations:
    banklab.konghq.com/example-only: "true"
    banklab.konghq.com/non-deployable: "true"
type: Opaque
stringData:
  example: REPLACE_WITH_SOPS_ENCRYPTED_VALUE

