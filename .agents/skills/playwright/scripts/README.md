# Playwright helper script

[`playwright_cli.sh`](playwright_cli.sh) is the wrapper used by the Playwright
skill to run the pinned command-line tool consistently.

Invocation rules and examples live in [`../SKILL.md`](../SKILL.md) and
[`../references/`](../references/).
