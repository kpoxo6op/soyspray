# Argo CD and Kubernetes application skill

This project-owned skill reviews a GitOps application as one operable system,
from Argo CD ownership through workload security and observability.

- [`SKILL.md`](SKILL.md) contains the architecture and review workflow.
- [`agents/openai.yaml`](agents/) contains the agent-facing name and prompt.

Use this skill when application boundaries, sync order, lifecycle controls, or
end-to-end verification need review.
