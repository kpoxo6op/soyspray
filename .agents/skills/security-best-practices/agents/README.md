# Agent interface

[`openai.yaml`](openai.yaml) provides the display metadata and default prompt
for the security best practices skill.

The full instructions live one level up in [`SKILL.md`](../SKILL.md).
