# Goal 003 Runtime Boundary

Local validation is required. Cluster mutation is optional and guarded. Goal 003 can be locally complete without cluster mutation, but it is not runtime-verified until apply and smoke tests pass. Goal 004 must not start until synthetic APIs are runtime-verified, unless the user explicitly accepts a local-only handoff.
