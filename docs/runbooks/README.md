# Runbooks

Runbooks describe repeatable operational responses.

Goal 000 only creates the template. Later goals should add runbooks for Kong
unavailable, high 5xx, high latency, bad release rollback, API key rotation,
certificate expiry, Redis failure, and tenant onboarding.

