# Documentation styles

[`extra.css`](extra.css) contains the small visual adjustments used by the
MkDocs operator guide. It is loaded through [`../../mkdocs.yml`](../../mkdocs.yml).

Application UI styles belong in
[`../../kubernetes/banklab/customer-web/app/`](../../kubernetes/banklab/customer-web/app/)
rather than this folder.
