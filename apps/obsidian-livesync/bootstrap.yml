---
- name: Bootstrap the existing Obsidian identities
  hosts: kube_control_plane[0]
  gather_facts: false
  become: true
  vars:
    kubeconfig_path: /etc/kubernetes/admin.conf
  tasks:
    - name: Prepare CouchDB and offsite writer inputs
      ansible.builtin.import_tasks: bootstrap-tasks.yml
