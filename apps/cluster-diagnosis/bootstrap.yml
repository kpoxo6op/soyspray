---
- name: Bootstrap the DeepSeek diagnosis identity
  hosts: kube_control_plane[0]
  gather_facts: false
  become: true
  vars:
    kubeconfig_path: /etc/kubernetes/admin.conf
  tasks:
    - name: Prepare the existing provider and delivery identities
      ansible.builtin.import_tasks: bootstrap-tasks.yml
