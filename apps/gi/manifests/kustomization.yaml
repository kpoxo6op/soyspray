apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gi
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - authentik-forward-auth.yaml
  - ingress.yaml
labels:
  - pairs:
      app.kubernetes.io/instance: gi
