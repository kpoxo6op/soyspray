apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: ai
spec:
  ports:
    - name: http
      port: 4096
      targetPort: relay
      protocol: TCP
  selector:
    app.kubernetes.io/name: ai-relay
