apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-proxy-set-headers-ai
  namespace: ai
data:
  X-Forwarded-Host: "$http_host"
---
apiVersion: v1
kind: Service
metadata:
  name: authentik-server-ai
  namespace: ai
spec:
  type: ExternalName
  externalName: authentik-server.authentik.svc.cluster.local
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-authentik-outpost
  namespace: ai
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - ai.soyspray.vip
      secretName: prod-cert-tls
  rules:
    - host: ai.soyspray.vip
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: authentik-server-ai
                port:
                  number: 80
