apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ai
  namespace: ai
spec:
  secretName: prod-cert-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ai.soyspray.vip
