apiVersion: v1
kind: Service
metadata:
  name: opencode-remote
  namespace: opencode-remote
spec:
  ports:
    - name: http
      port: 4096
      targetPort: relay
      protocol: TCP
  selector:
    app.kubernetes.io/name: opencode-relay
