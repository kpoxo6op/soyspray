# API Template

This template shows the minimum shape expected for future API products.

Goal 000 does not create real tenant APIs. The files here exist so later goals
have a stable pattern to extend.

