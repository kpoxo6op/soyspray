# API workloads

This folder contains sample APIs used to practise gateway routing, policy, and
tenant ownership.

- [`synthetic-bank/`](synthetic-bank/) is the deployable API collection used by
  the Kong bank lab.

These services return synthetic responses only. No real customer or banking
data belongs in this folder.
