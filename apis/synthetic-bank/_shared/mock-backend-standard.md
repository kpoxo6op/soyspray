# Mock Backend Standard

Use `nginxinc/nginx-unprivileged:1.27-alpine` with a ConfigMap-mounted nginx server block. Backends serve deterministic synthetic responses only and expose health markers on port 8080.
