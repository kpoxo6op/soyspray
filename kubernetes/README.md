# Kubernetes goal manifests

This folder holds goal-scoped runtime manifests and rollback fixtures.

Treat apply and rollback paths as controlled change material. For a read-only
tour, use the runbooks and smoke commands instead.
