---
- name: Apply Immich Database B
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    namespace: argocd
    definition: "{{ lookup('file', playbook_dir + '/argocd/applications/database/cnpg/immich-db-b-application.yaml') }}"
  tags: [immich-db-b]

