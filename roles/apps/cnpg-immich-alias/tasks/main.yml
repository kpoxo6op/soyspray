---
- name: Apply CNPG Immich Alias ApplicationSet
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    namespace: argocd
    definition: "{{ lookup('file', playbook_dir + '/argocd/applications/database/cnpg/apps/applicationset-immich-alias.yaml') }}"
  tags: [cnpg-immich-alias]

