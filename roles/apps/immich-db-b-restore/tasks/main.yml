---
- name: Apply Immich Database B Restore
  kubernetes.core.k8s:
    state: present
    kubeconfig: "{{ kubeconfig_path }}"
    namespace: argocd
    definition: "{{ lookup('file', playbook_dir + '/argocd/applications/database/cnpg/immich-db-b-restore-application.yaml') }}"
  tags: [immich-db-b-restore]

