# Live TV role defaults

`live_tv_enabled` controls the full live television stack. `live_tv_target_revision` selects the pushed Git revision for Argo CD.

The user-name variables set stable native account names. The role generates and preserves the passwords in Kubernetes Secrets.
