# Autism traits role tasks

[`enabled.yml`](enabled.yml) applies the Argo CD Application and propagates the
selected Git revision.

[`disabled.yml`](disabled.yml) stops automated sync, clears an active operation,
and removes the Application by its API identity.
